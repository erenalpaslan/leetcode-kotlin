package main

import interview.*
import interview.ClimbingStairs.climbStairs
import interview.MergeTwoSortedList.printNode

fun main(args: Array<String>) {
    val single = SingleNumber.singleNumber(intArrayOf(2, 2, 1, 4, 1))
    println("==> $single")
}